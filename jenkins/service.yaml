apiVersion: v1
kind: Service
metadata:
  name: svc-mycicd-jenkins
spec:
  type: NodePort
  selector:
    app: app-mycicd-jenkins
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30001
